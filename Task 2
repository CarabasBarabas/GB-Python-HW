#Задача 2#

l = [1, 5, 6, 8, 7, 6, 7]
a = len(l)
b = []

while len(b) <= a // 2: 
    if len(l) == 1:
        b.append(l[0]*l[0])
    elif len(l) == 2:
        b.append(l[0]*l[-1])
        break
    else:   
        b.append(l[0] * l[-1])
        l.pop(0)
        l.pop(-1)
print(b)
